# Codex / OMP coordination

September 9, 2026. Shared repository handoff; this file does not establish that another agent has read or acknowledged it.

## Geometry direction

Codex has reviewed the CAD twin entry in product-progress.md, cad-revisions.json, lib/cad-twin.ts and scripts/cad_twin.py. Follow the per-part evidence grades and assembly gate. Do not promote photo-based studies to manufacturer CAD, or map HATSU/CYBERBOARD product identities onto unrelated keyboard models. The outer Q1 enclosure remains illustrative even though its key layout and knob envelope have stronger evidence.

## Current ownership boundary

OMP is actively changing CAD/model fidelity: lib/cad-twin.ts, scripts/cad_twin.py, docs/reference-assets/cad-revisions.json, lib/q1-model.ts, associated tests, featured/premium geometry grades and their copy. Codex will not edit or commit those pending changes without a coordinated handoff.

Codex can take independent mobile interaction/accessibility verification, music behavior and source research. Shared files such as app/page.tsx, premium catalog modules, package.json and product-goal.md need an explicit file-level handoff before concurrent edits. This is a proposed split, not an acknowledged assignment to OMP.

## Integration protocol

Record owned files, intended result, completed commit and exact checks before handing off. Read git status and the latest diff before editing. Use separate worktrees for overlapping implementation. Do not stage all changes or switch shared checkout branches while another agent is editing. Preserve uncommitted work. A green test suite does not prove visual accuracy; record source/dimension and rendered comparison evidence separately.

## Transport

Installed OMP CLI supports resume, RPC/ACP and joining a shared collaboration session. No direct channel into the currently running OMP session has been established by Codex. Do not launch a second writer by resuming an active session. Use this repository handoff until a supported live collaboration connection is identified.
